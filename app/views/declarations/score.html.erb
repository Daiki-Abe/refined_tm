<%= render 'shared/header' %>

<div class = "title">
  <h1 class = "title__main-title">Today's Action Record</h1>
  <h2 class = "title__sub-title">〜 今日を振り返り、明日からの時間の使い方に生かそう！ 〜</h2>
</div>

<table align = "center" >
  <thead>
    <tr>
      <th>When</th>
      <th>What</th>
      <th>Why</th>
      <th>Time</th>
      <th>Result</th>
    </tr>
  </thead>

  <tbody>
  <% @declarations.each do |declaration| %>
    <% if declaration.user_id == current_user.id %>
      <tr>
        <td><%= declaration.start %> - <%= declaration.end %></td>
        <td><%= declaration.what %></td>
        <td><%= declaration.why %></td>
        <td><%= declaration.time %></td>
        <td><%= declaration.score %></td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>

<div class = "record__field">
  <%= link_to "Start!!", new_declaration_path, class: "link__start" %>
  <%= link_to "今日を終了", "/declarations/#{@declarations.ids}", method: :delete, class: "link__delete" %>
</div>